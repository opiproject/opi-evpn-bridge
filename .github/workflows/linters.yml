name: Linters

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  call:
    uses: opiproject/actions/.github/workflows/linters.yml@main
    secrets: inherit
